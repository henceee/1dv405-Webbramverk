<% if flash[:notice] %>
    <div class="notice">
        <%= flash[:notice] %>
    </div>
<% end %>

<h1> Your API:s</h1>

<ul>
    <%@user.applications.each do |app| %>
    <li>
    <p><%= link_to(app.name, showapp_path(app)) %> <%= link_to("Delete", destroyapp_path(app)) %></p>
    </li>
    <% end %>
</ul>


<p><%= link_to("Create new", new_api_path) %></p>
<p><%= link_to("Log  Out", logout_path) %></p>